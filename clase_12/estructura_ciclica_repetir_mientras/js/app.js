let contadorA = 1;

while (contadorA <= 10) {
    document.write("<p>Iteración A " + contadorA + "</p>");
    contadorA++; // ++ incrementando en 1
}

let contadorB = 0;
while (contadorB <= 5) {
    document.write("<p>Iteración B " + contadorB + "</p>");
    contadorB += 2; // incrementando en 2 (contadorB + 2)
}


document.write("<p>Fin de instrucciones</p>");